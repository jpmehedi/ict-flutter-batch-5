import 'package:flutter/material.dart';

class Gadgats extends StatelessWidget {
  const Gadgats({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.pink,
      body: null,
    );
  }
}